query getCurrencies {
  currencies(pagination: { limit: -1 }) {
    data {
      id
      attributes {
        code
        symbol
        rate_to_usd
        display_decimals
      }
    }
  }
}
